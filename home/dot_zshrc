#!/usr/bin/env zsh

#
# Path settings
#
typeset -gU path fpath
path=(
    $path
    /usr/local/{,s}bin(N-/)
    ${HOME}/.local/bin(N-/)
)
fpath=(
    $fpath
)

#
# sheldon initialization
#
eval "$(sheldon source)"
